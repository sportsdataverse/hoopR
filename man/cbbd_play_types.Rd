% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cbbd_plays.R
\name{cbbd_plays_game}
\alias{cbbd_plays_game}
\alias{cbbd_plays_player}
\alias{cbbd_plays_team}
\alias{cbbd_plays_date}
\alias{cbbd_plays_tournament}
\alias{cbbd_play_types}
\title{\strong{CBD Plays by Game}}
\usage{
cbbd_plays_game(game_id, shooting_plays_only = FALSE)

cbbd_plays_player(
  player_id,
  season = most_recent_mbb_season(),
  shooting_plays_only = FALSE
)

cbbd_plays_team(
  season = most_recent_mbb_season(),
  team,
  shooting_plays_only = FALSE
)

cbbd_plays_date(date, shooting_plays_only = FALSE, utc_offset = NULL)

cbbd_plays_tournament(
  tournament,
  season = most_recent_mbb_season(),
  shooting_plays_only = FALSE
)

cbbd_play_types()
}
\arguments{
\item{game_id}{(\emph{integer} required): CollegeBasketballData game id. See
\code{\link[=cbbd_games]{cbbd_games()}}.}

\item{shooting_plays_only}{(\emph{logical} optional): If \code{TRUE}, return only
shooting plays. Defaults to \code{FALSE}.}

\item{player_id}{(\emph{integer} required): Athlete id.}

\item{season}{(\emph{integer} required): Season, 4-digit ending-year (e.g. \code{2024}).
Defaults to \code{most_recent_mbb_season()}.}

\item{team}{(\emph{character} required): Team name (e.g. \code{Duke}).}

\item{date}{(\emph{character} required): Date-time in ISO 8601 format
(e.g. \verb{2024-02-01T00:00:00.000Z}).}

\item{utc_offset}{(\emph{numeric} optional): UTC offset (hours) for the date.}

\item{tournament}{(\emph{character} required): Tournament name (e.g. \code{NCAA}).}
}
\value{
A \code{hoopR_data} tibble with one row per play (key columns; \code{shot_info}
is flattened to \verb{shot_info_*}, \code{participants}/\code{on_floor} are list-columns):

\if{html}{\tabular{lll}{
      col_name \tab types \tab description \cr
      id \tab integer \tab Play id. \cr
      game_id \tab integer \tab Game id. \cr
      season \tab numeric \tab Season (4-digit ending-year). \cr
      play_type \tab character \tab Play type description. \cr
      team_id \tab integer \tab Team id of the team on the play. \cr
      team \tab character \tab Team on the play. \cr
      period \tab integer \tab Period number. \cr
      clock \tab character \tab Game clock. \cr
      seconds_remaining \tab integer \tab Seconds remaining in the period. \cr
      home_score \tab integer \tab Home score after the play. \cr
      away_score \tab integer \tab Away score after the play. \cr
      scoring_play \tab logical \tab Whether the play scored. \cr
      shooting_play \tab logical \tab Whether the play was a shot. \cr
      score_value \tab numeric \tab Point value of the play. \cr
      play_text \tab character \tab Play description text. \cr
   }}
\if{latex}{See the HTML help or pkgdown reference for the column table.}

A \code{hoopR_data} tibble with one row per play (same columns as
\code{\link[=cbbd_plays_game]{cbbd_plays_game()}}).

A \code{hoopR_data} tibble with one row per play (same columns as
\code{\link[=cbbd_plays_game]{cbbd_plays_game()}}).

A \code{hoopR_data} tibble with one row per play (same columns as
\code{\link[=cbbd_plays_game]{cbbd_plays_game()}}).

A \code{hoopR_data} tibble with one row per play (same columns as
\code{\link[=cbbd_plays_game]{cbbd_plays_game()}}).

A \code{hoopR_data} tibble with one row per play type:

\if{html}{\tabular{lll}{
      col_name \tab types \tab description \cr
      id \tab integer \tab Play type id. \cr
      name \tab character \tab Play type name. \cr
   }}
\if{latex}{See the HTML help or pkgdown reference for the column table.}
}
\description{
\strong{Get play-by-play data for a single game from the CollegeBasketballData API.}

\strong{Get play-by-play data for a single player from the CollegeBasketballData API.}

\strong{Get play-by-play data for a team-season from the CollegeBasketballData API.}

\strong{Get play-by-play data for all games on a date from the CollegeBasketballData API.}

\strong{Get play-by-play data for a tournament from the CollegeBasketballData API.}

\strong{Get the list of play types from the CollegeBasketballData API.}
}
\examples{
\donttest{
  try(cbbd_plays_game(game_id = 5881))
}
\donttest{
  try(cbbd_plays_player(player_id = 160, season = 2024))
}
\donttest{
  try(cbbd_plays_team(season = 2024, team = "Duke"))
}
\donttest{
  try(cbbd_plays_date(date = "2024-02-01T00:00:00.000Z"))
}
\donttest{
  try(cbbd_plays_tournament(tournament = "NCAA", season = 2024))
}
\donttest{
  try(cbbd_play_types())
}
}
\concept{CBD Plays Functions}
\keyword{CBD}
\keyword{Plays}

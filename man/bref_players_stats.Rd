% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bref_players_stats.R
\name{bref_players_stats}
\alias{bref_players_stats}
\title{\strong{Basketball-Reference Player Season Stats}}
\usage{
bref_players_stats(season = most_recent_nba_season(), table = "per_game")
}
\arguments{
\item{season}{(\emph{integer} required): Season, in 4-digit ending-year format
(e.g. \code{2024} for the 2023-24 season). Defaults to \code{most_recent_nba_season()}.}

\item{table}{(\emph{character} optional): Which stat table to return. One of
\code{per_game} (default), \code{totals}, \code{advanced}, \code{per_minute} (per 36 minutes),
or \code{per_poss} (per 100 possessions).}
}
\value{
A \code{hoopR_data} tibble with one row per player (columns vary by
\code{table}; common identifying columns shown -- column names are
Basketball-Reference \code{data-stat} keys):\tabular{lll}{
   col_name \tab types \tab description \cr
   ranker \tab integer \tab Row rank. \cr
   player \tab character \tab Player name. \cr
   age \tab integer \tab Player age on Feb 1 of the season. \cr
   team_id \tab character \tab Team abbreviation (\code{TOT} for players on >1 team). \cr
   pos \tab character \tab Position. \cr
   g \tab integer \tab Games played. \cr
   gs \tab integer \tab Games started. \cr
   mp_per_g \tab numeric \tab Minutes (per_game table) / \code{mp} total (totals table). \cr
   pts_per_g \tab numeric \tab Points (scaled to the chosen \code{table}). \cr
   season \tab integer \tab Season (echoes the \code{season} argument). \cr
}


The \code{advanced} table adds \code{per}, \code{ts_pct}, \code{usg_pct}, \code{ws}, \code{bpm}, \code{vorp},
etc.; \code{totals}/\code{per_minute}/\code{per_poss} return the same box categories scaled
accordingly.
}
\description{
\strong{Get player season statistics from
\href{https://www.basketball-reference.com}{Basketball-Reference}.}

Scrapes the league-wide player stat table for a season. No API key is
required. Choose the stat table with \code{table}. Data is available back to the
1947 (BAA) season for most tables.

Basketball-Reference rate-limits aggressive scraping (~20 requests/minute) --
space repeated calls with \code{Sys.sleep()}.
}
\examples{
\donttest{
  try(bref_players_stats(season = 2024, table = "per_game"))
}
}
\seealso{
Other Basketball-Reference Functions: 
\code{\link{bref_awards}()},
\code{\link{bref_draft}()},
\code{\link{bref_injuries}()},
\code{\link{bref_player_bios}()},
\code{\link{bref_player_game_log}()},
\code{\link{bref_standings}()},
\code{\link{bref_team_roster}()},
\code{\link{bref_teams_stats}()}
}
\concept{Basketball-Reference Functions}
\keyword{Basketball-Reference}

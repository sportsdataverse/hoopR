% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_nba_stats.R
\name{update_nba_stats_db}
\alias{update_nba_stats_db}
\title{\strong{Build/update hoopR NBA Stats database}}
\usage{
update_nba_stats_db(
  dbdir = ".",
  dbname = "hoopR_db",
  datasets = NULL,
  seasons = TRUE,
  db_connection = NULL
)
}
\arguments{
\item{dbdir}{Directory in which the database is or shall be located.}

\item{dbname}{File name of an existing or desired SQLite database within
\code{dbdir}.}

\item{datasets}{Character vector of dataset shortnames (see Details).}

\item{seasons}{Optional integer vector of seasons to load. Defaults to all
available seasons (\code{seasons = TRUE}).}

\item{db_connection}{A \code{DBIConnection} object. When supplied, \code{dbdir} and
\code{dbname} are ignored.}
}
\value{
Invisibly returns \code{TRUE} on success.
}
\description{
\code{update_nba_stats_db()} writes one or more NBA Stats API
datasets into a database. Mirrors the NBA / MBB \verb{update_*_db()} shape
but points at the \verb{load_nba_stats_*} family. Each dataset is written
to its own table, named \verb{nba_stats_<dataset>}.
}
\details{
Unlike \code{update_nba_db()}, there is no historical "play-by-play table"
default to preserve, so \code{datasets} is required.

Valid \code{datasets} values:
\code{"coaches"}, \code{"draft"}, \code{"game_lineups"}, \code{"game_rosters"}, \code{"lineups"},
\code{"officials"}, \code{"pbp"}, \code{"player_boxscores"}, \code{"player_game_logs"},
\code{"player_season_stats"}, \code{"possessions"}, \code{"rosters"}, \code{"schedule"},
\code{"shots"}, \code{"standings"}, \code{"team_boxscores"}, \code{"team_season_stats"}.
}

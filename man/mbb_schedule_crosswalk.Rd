% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mbb_crosswalk.R
\name{mbb_schedule_crosswalk}
\alias{mbb_schedule_crosswalk}
\title{\strong{Get the MBB cross-source schedule crosswalk}}
\usage{
mbb_schedule_crosswalk(
  season = most_recent_mbb_season(),
  include_kenpom = FALSE
)
}
\arguments{
\item{season}{Season year (4-digit, e.g. \code{2025}). Defaults to
\code{most_recent_mbb_season()}.}

\item{include_kenpom}{Logical. When \code{TRUE} AND \code{KP_USER} is set,
attempts to enrich \code{kp_game_id} via per-team \code{kp_team_schedule()} calls.
Default \code{FALSE}.}
}
\value{
A \code{hoopR_data} tibble, one row per game:\tabular{lll}{
   col_name \tab types \tab description \cr
   season \tab integer \tab Season year. \cr
   game_date \tab Date \tab ET game date. \cr
   home_espn_team_id \tab integer \tab ESPN home team id (NA for bart-only rows). \cr
   away_espn_team_id \tab integer \tab ESPN away team id (NA for bart-only rows). \cr
   espn_game_id \tab character \tab ESPN game id (NA for bart-only rows). \cr
   bart_muid \tab character \tab Torvik muid (NA for espn-only rows). \cr
   bart_team1 \tab character \tab Torvik team1 name (NA for espn-only rows). \cr
   bart_team2 \tab character \tab Torvik team2 name (NA for espn-only rows). \cr
   bart_winner \tab character \tab Torvik winner name (NA for espn-only rows). \cr
   kp_game_id \tab character \tab KenPom game id (NA unless kenpom enabled). \cr
   fox_game_id \tab character \tab Fox game id (NA placeholder). \cr
   yahoo_game_id \tab character \tab Yahoo game id (NA placeholder). \cr
   match_method \tab character \tab "both"/"espn_only"/"bart_only". \cr
   match_confidence \tab numeric \tab 1 for matched, NA for unmatched. \cr
}
}
\description{
Build a wide, one-row-per-game crosswalk linking ESPN and Bart Torvik
(barttorvik.com) game identifiers for an MBB season. Fox Sports and Yahoo
game IDs are NA placeholders. KenPom game IDs are optionally enriched when
\code{include_kenpom = TRUE} and credentials are set (see below). Dates are
reduced to Eastern-Time game dates before joining; Torvik \code{team1}/\code{team2}
are unordered (the join uses a sorted team-pair key, so home/away from the
Torvik side is not preserved). Games where either Torvik team name cannot be
resolved to an ESPN id via \code{mbb_team_crosswalk()} are kept as \code{bart_only}
rows.

\strong{KenPom (optional):} KenPom requires a paid subscription. Set the
environment variables \code{KP_USER} (email) and \code{KP_PW} (password) and pass
\code{include_kenpom = TRUE}. When \code{include_kenpom = FALSE} (the default) or
when credentials are absent, \code{kp_game_id} is left as \code{NA} and no network
calls to KenPom are made. Errors from individual team schedule calls are
silently dropped so the function always returns a complete crosswalk even
when partial KenPom data is unavailable.
}
\examples{
\donttest{
  try(mbb_schedule_crosswalk(season = 2025))
}
}
\seealso{
Other MBB Crosswalk Functions: 
\code{\link{mbb_player_crosswalk}()},
\code{\link{mbb_team_crosswalk}()}
}
\author{
Saiem Gilani
}
\concept{MBB Crosswalk Functions}

# Internal: fetch a URL and return the parsed HTML document

Internal: fetch a URL and return the parsed HTML document

## Usage

``` r
.ext_html(url)
```

## Arguments

- url:

  Full URL.

## Value

An `xml_document` (rvest).

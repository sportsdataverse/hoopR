# **rejoin schedules (when used from league game finder)**

**rejoin schedules (when used from league game finder)**

## Usage

``` r
rejoin_schedules(df)
```

## Arguments

- df:

  data frame pulled from nba_leaguegamefinder()
